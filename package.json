{
  "name": "mqtt-test",
  "version": "0.0.1",
  "description": "simple MQTT test",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mqtt"
  ],
  "author": "Alessandro Sebastiani",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^1.14.1"
  }
}
